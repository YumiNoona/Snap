// Export module — FFmpeg subprocess pipeline for rendering the final video.
// Reads recorded video + JSON sidecar, applies pan/zoom keyframes,
// composits cursor overlay, background, padding, shadow, and renders
// the timeline edits to an output video file.
// Implemented later.
